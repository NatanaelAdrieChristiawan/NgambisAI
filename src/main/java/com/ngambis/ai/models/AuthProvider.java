package com.ngambis.ai.models;

/**
 * Enumeration of authentication providers supported by the system.
 * LOCAL = email/password registration, GOOGLE = Google OAuth2 login.
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE
}
